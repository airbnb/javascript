{
  "rules": {
    // disabled because I find it tedious to write tests while following this
    // rule
    "no-shadow": 0,
    // tests uses `t` for tape
    "id-length": [1, {"min": 1, "properties": "never", "exceptions": ["t"]}],
    "import/no-extraneous-dependencies": [1, {
      "devDependencies": true
    }],
  }
}
