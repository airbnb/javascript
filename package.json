{
  "name": "ads-docs",
  "version": "0.0.1",
  "engines" : { "node" : "6.11.0" },
  "description": "chrysler sdp prototypes docs",
  "private": true,
  "scripts": {
    "start": "gitbook install && gitbook serve",
    "build": "gitbook build"
  },
  "dependencies": {
    "gitbook-cli": "*"
  }
}
